<div class="header-container w-100 mb-4 px-3 d-flex justify-content-between align-items-center">
  <div class="left-container">
    <i class="fa-solid fa-sliders" style="color: #000000;" class='header-symbol'></i>
    <i class="fa-solid fa-bookmark" style="color: #000000;" class='header-symbol'></i>
  </div>

  <% if user_signed_in? %>
    <span class="me-2">Bonjour <%= current_user.pseudo %> !</span>
    <%= link_to "Déconnexion", destroy_user_session_path, data: { turbo_method: :delete }, class: "btn btn-outline-danger" %>
  <% else %>
    <%= link_to "Connexion", new_user_session_path, class: "btn btn-outline-primary" %>
  <% end %>

</div>
