<div class="header-container w-100 mb-3 px-3 d-flex justify-content-between align-items-center">
  <div class="left-container">
    <i class="fa-solid fa-bookmark" style="color: #000000;" class='header-symbol'></i>
  </div>
  
  <%= image_tag 'logo.svg', class: 'logo-header' %>

  <% if user_signed_in? %>
    <span class="me-2"> <%= current_user.pseudo %> </span>
    <%= link_to "Déconnexion", destroy_user_session_path, data: { turbo_method: :delete }, class: "log-out btn btn-outline-primary btn-sm" %>
  <% else %>
    <%= link_to "Connexion", new_user_session_path, class: "btn btn-outline-primary btn-sm" %>
  <% end %>
</div>
