<%= simple_form_for [@feed, Message.new], html: { class: "d-flex w-100" } do |f| %>
  <div class="form-container w-100">
    <div class="input-message-container w-100">
      <%= f.text_area :body,
          placeholder: "Votre message...",
          class: "form-textarea-message mb-0",
          rows: 1,
          data: { controller: "autosize" } %>

      <div class="form-submit-message">
        <%= f.submit "Envoyer", class: "btn-form-message btn btn-primary rounded-pill" %>
      </div>
    </div>
  </div>
<% end %>
