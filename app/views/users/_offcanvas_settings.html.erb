<div class="header-container w-100 d-flex justify-content-between align-items-center mt-5">
  <div class="titre-container d-flex align-items-center gap-5">
    <button type="button" class="btn text-reset" data-bs-dismiss="offcanvas" aria-label="Close">
      <%= image_tag 'Left_arrow.svg', class: 'header-arrow' %>
    </button>
    <div class="titre-prfile gap-1 text-center"
          style="margin-left: 2.5rem;">
      <h2 class="header-pseudo text-light mb-0">Parametres</h2>
    </div>
  </div>
</div>
<div class="offcanvas-body d-flex flex-column align-items-center justify-content-start gap-3 mt-5" style="width: 100%;">
  <% if current_user.userable_type == "Artist" %>
    <%= link_to "Modifier mon profil", edit_artist_path(current_user.userable), class: "btn btn-primary w-75 text-light shadow" %>
  <% end %>

  <%= form_with url: destroy_user_session_path, method: :delete, data: { turbo: false }, class: "w-75" do %>
    <%= submit_tag "Déconnexion", class: "btn btn-primary w-100 text-light shadow" %>
  <% end %>
</div>
