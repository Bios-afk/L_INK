<!-- 🔽 Filtres principaux -->
<div data-controller="category-filter">
<div class="offcanvas offcanvas-bottom custom-height"
     tabindex="-1"
     id="filtersOffcanvas"
     aria-labelledby="filtersOffcanvasLabel"
     data-category-filter-selected-categories-value='<%= (params[:category_ids] || []).to_json %>'>
  <div class="offcanvas-header">
    <h5 class="offcanvas-title" id="filtersOffcanvasLabel">Rechercher</h5>
    <button type="button" class="btn-close" data-bs-dismiss="offcanvas" aria-label="Fermer"></button>
  </div>

  <div class="offcanvas-body">
    <hr class="hr">

    <%= form_with url: artists_path, method: :get, local: true do |f| %>

      <!-- 🎨 Bouton d’ouverture du offcanvas Catégorie -->
      <div
        class="mb-3 d-flex justify-content-between align-items-center"
        style="cursor: pointer;"
        data-bs-toggle="offcanvas"
        data-bs-target="#categoryOffcanvas"
        aria-controls="categoryOffcanvas"
      >
        <label class="form-label mb-0">Catégories</label>
        <span class="fs-4">&rsaquo;</span> <!-- flèche à droite -->
      </div>

      <!-- 🟦 Affichage des catégories sélectionnées -->
      <div
        id="selected-categories"
        class="mb-3"
        data-category-filter-target="container"
      ></div>

      <hr class="hr">

      <!-- 📍 Ville -->
      <div class="mb-3">
        <label for="city-autocomplete" class="form-label">Ville</label>
        <input type="text" id="city-autocomplete" class="form-control" placeholder="Ex : Paris">
      </div>

      <!-- 🟦 Affichage des villes sélectionnées -->
      <div id="selected-cities" class="mb-3"></div>

      <!-- ⭐ Note -->
      <div class="mb-3">
        <%= f.label :rating, "Note minimale" %>
        <%= f.select :rating, options_for_select((1..5).to_a.reverse.map { |n| ["#{n}★ et plus", n] }), include_blank: "Toutes", class: "form-select" %>
      </div>

      <!-- ✅ Bouton appliquer -->
      <%= f.submit "Appliquer les filtres", class: "btn btn-primary w-100" %>
    <% end %>
  </div>
</div>

<!-- 🔽 Offcanvas secondaire pour sélection des catégories -->
<div class="offcanvas offcanvas-bottom custom-height"
     tabindex="-1"
     id="categoryOffcanvas"
     aria-labelledby="categoryOffcanvasLabel">
  <div class="offcanvas-header">
    <h5 class="offcanvas-title" id="categoryOffcanvasLabel">Choisissez vos catégories</h5>
    <button type="button" class="btn-close" data-bs-dismiss="offcanvas" aria-label="Fermer"></button>
  </div>
  <div class="offcanvas-body" style="overflow-y: auto; max-height: 80vh;">
    <% Category.all.each do |category| %>
      <div class="form-check mb-2">
        <input
          class="form-check-input"
          type="checkbox"
          value="<%= category.id %>"
          id="category_<%= category.id %>"
          data-category-filter-target="checkbox"
        >
        <label class="form-check-label" for="category_<%= category.id %>">
          <%= category.name %>
        </label>
      </div>
    <% end %>

    <button type="button" class="btn btn-primary w-100" id="applyCategories" data-action="click->category-filter#applyCategories">Valider</button>
  </div>
</div>
</div>
